import 'package:flutter/material.dart';

class JobSeekerWebProfile extends StatelessWidget {
  const JobSeekerWebProfile({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('jobseekerweb-profile screen')));
}
