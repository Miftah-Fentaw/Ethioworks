import 'package:flutter/material.dart';

class EmployerMobileProfile extends StatelessWidget {
  const EmployerMobileProfile({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('employermobile-profile screen')));
}
