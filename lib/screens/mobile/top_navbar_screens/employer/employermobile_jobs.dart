import 'package:flutter/material.dart';

class EmployerMobileJobs extends StatelessWidget {
  const EmployerMobileJobs({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('employermobile-jobs screen')));
}
